---
const { text, href, asLI } = Astro.props;
---

{
  asLI ? (
    <li>
      <a class="underline hover:no-underline" href={href}>
        {text}
      </a>
    </li>
  ) : (
    <a class="underline hover:no-underline" href={href}>
      {text}
    </a>
  )
}
